string reverseWord(string str){
    int start = 0;
    int end = str.length()-1;
    for(int i=0;i<end;i++)
    {
        swap(str[start],str[end]);
        start++;
        end--;
    }
    return str;
}
